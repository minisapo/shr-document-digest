# shr-document-digest プロジェクトルール

## 概要
SmartHR（勤怠・労務管理システム）にPlaywrightでログインし、「文書配付」の受信ボックス一覧をスクレイピングしてCSV化する。
さらに一番上の文書を開き、内容を読み取ってダイジェスト（要約）テキストを生成する。
PAD（Power Automate Desktop）で行っていた定型の画面操作を、Python + Playwrightで再現する最初のトライアル。

## 機能要件

1. **ログイン**
   - URL: `.env` の `SMARTHR_URL`
   - ログインID: `.env` の `SMARTHR_LOGIN_ID`（社員番号 または ログイン用メールアドレス）
   - パスワード: `.env` の `SMARTHR_PASSWORD`
   - ブラウザは **Chrome (channel="chrome")** を起動し、**ウィンドウ最大化**で表示する

2. **画面遷移**
   - ログイン後、左メニューから「文書配付」へ遷移する

3. **受信ボックス一覧のスクレイピング**
   - 文書配付の受信ボックスに表示されている一覧（文書名・配付日・既読/未読などの列）を取得
   - CSVとして出力（列構成はスクレイピング時に実際のDOMを見て確定する）

4. **文書の閲覧とダイジェスト化**
   - 一覧の一番上（最新）の文書ページへ遷移
   - 表示された書類の内容（テキスト、またはPDF/画像であればOCRやテキスト抽出）を読み取る
   - Anthropic API（`claude-sonnet-4-6`）でダイジェスト（要約）テキストを生成する
     - 参考: 過去の[[morning-news-automation]]プロジェクトと同じ要約アプローチ

5. **出力**
   - 2つのアウトプットファイルは **デスクトップ** に保存する
     - `文書配付一覧_YYYYMMDD.csv`
     - `文書ダイジェスト_YYYYMMDD.txt`

6. **終了処理**
   - 処理完了後、SmartHRからログアウトし、ブラウザを閉じる

## 技術方針

- 言語: Python（venv使用）
- ブラウザ自動化: Playwright（channel="chrome"）
- 要約: Anthropic API（anthropic SDK）
- 環境変数管理: python-dotenv、`.env`はgit管理外
- まずは `playwright codegen` で実際のSmartHR画面の要素（セレクタ）を確認してから実装する
  - ※ codegenでログイン操作を録画する際は、パスワードの打鍵がターミナル履歴やスクリプトに平文で残らないよう注意する

## 進め方の希望

- 一つずつのステップ（ログイン→文書配付遷移→一覧取得→CSV化→文書閲覧→要約→保存→ログアウト）を確認しながら進める
- PAD経験があるので「PADならこのステップ」という対応イメージがあると理解しやすい
- コミットは機能単位で細かく分ける（前回の朝ニュースプロジェクトと同じ進め方）
